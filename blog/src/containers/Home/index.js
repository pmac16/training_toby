import React from "react";
import Header from "../../components/Header";
import "./style.css";

const Home = (props) => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default Home;
